<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.jack.core.dao.product.BrandDao">
	
	<resultMap type="Brand" id="brand">
		<!-- 把字段名称不一致的写在这里就可以了，没有必要都写出来  column:数据库字段  property:javaBean字段-->
		<result column="img_url" property="imgUrl"/>
		<result column="is_display" property="isDisplay"/>
	</resultMap>
	
	<!-- 查询  resultMap数据库表字段的名称和javaBean不同 -->
	<select id="selectBrandListByQuery" parameterType="BrandQuery" resultMap="brand">
		select id , name ,description,img_url,sort,is_display
		from sts_brand
		<where>
			<if test="name != null">
				name like "%"#{name}"%"
			</if>
			<if test="isDisplay != null">
				and is_display = #{isDisplay}
			</if>
		</where>
		<if test="startRow != null">
			limit #{startRow},#{pageSize}
		</if>
	</select>
	
	<!-- 查询总条数 -->
	<select id="selectCount" parameterType="BrandQuery" resultType="Integer">
		select count(1)
		from sts_brand
		<where>
			<if test="name != null">
				name like "%"#{name}"%"
			</if>
			<if test="isDisplay != null">
				and is_display = #{isDisplay}
			</if>
		</where>
	</select>
	
	<!--通过Id查询  -->
	<select id="selectBrandById" parameterType="Long" resultMap="brand">
		select id , name ,description,img_url,sort,is_display
		from sts_brand
		<where>
			id=#{id}
		</where>
	</select>
</mapper>
